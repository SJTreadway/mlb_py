# TODO List

## Store all numeric values as int or float rather than string. Currently is causing issues deleting duplicate rows in existing data.

## Fix Bpen (rollsum & mod) features. Seems a lot (maybe all) are using default values rather than actual.